# 心理学Bot 服务器部署说明

## 一、部署要求

- Linux 服务器（Ubuntu 20.04+ / CentOS 7+ / Debian 11+ 均可）
- 至少 1GB 内存
- 已安装 Docker 和 Docker Compose
- 服务器防火墙/安全组放行 8000 端口

## 二、快速部署（三步搞定）

### 第 1 步：上传文件到服务器

把整个文件夹上传到服务器 `/opt/psychobot/` 目录：

```bash
scp -r ./psychobot-server/ root@你的服务器IP:/opt/psychobot/
```

或者用 FTP / 宝塔面板 / 其他工具上传都行。

### 第 2 步：SSH 登录服务器，运行部署脚本

```bash
ssh root@你的服务器IP
cd /opt/psychobot
chmod +x deploy.sh start.sh stop.sh
./deploy.sh
```

脚本会自动完成：
- 检查并安装 Docker（如果没有）
- 构建 Docker 镜像
- 启动容器
- 尝试开放防火墙端口

### 第 3 步：浏览器访问

```
http://你的服务器IP:8000
```

看到聊天界面就说明部署成功了。

## 三、常用操作

### 启动服务

```bash
cd /opt/psychobot
./start.sh
```

### 停止服务

```bash
cd /opt/psychobot
./stop.sh
```

### 查看实时日志

```bash
cd /opt/psychobot
docker compose -f docker-compose.server.yml logs -f
```

### 重启服务

```bash
cd /opt/psychobot
./stop.sh && ./start.sh
```

### 更新代码后重新部署

```bash
cd /opt/psychobot
./stop.sh
docker compose -f docker-compose.server.yml build --no-cache
./start.sh
```

## 四、修改 API Key 或模型

编辑 `.env` 文件：

```bash
nano .env
```

内容说明：

```text
OPENAI_API_KEY=你的API密钥
OPENAI_BASE_URL=API地址
OPENAI_MODEL=模型名称
```

修改后重启生效：

```bash
./stop.sh && ./start.sh
```

## 五、设置开机自启（可选）

如果希望服务器重启后服务自动恢复：

```bash
# 复制 systemd 配置
sudo cp psychobot.service /etc/systemd/system/

# 启用开机自启
sudo systemctl daemon-reload
sudo systemctl enable psychobot

# 之后可以用 systemctl 管理
sudo systemctl start psychobot
sudo systemctl stop psychobot
sudo systemctl status psychobot
```

## 六、修改端口（可选）

默认端口是 8000。如果需要改成其他端口（比如 9000）：

编辑 `docker-compose.server.yml`：

```yaml
ports:
  - "9000:8000"    # 改左边这个数字
```

然后重启：

```bash
./stop.sh && ./start.sh
```

记得在防火墙/安全组里也放行新端口。

## 七、云服务器安全组配置

### 阿里云 / 腾讯云 / 华为云

登录云控制台 → 找到你的服务器 → 安全组规则 → 添加入方向规则：

| 协议 | 端口范围 | 授权对象 | 说明 |
|------|----------|----------|------|
| TCP  | 8000     | 0.0.0.0/0 | 允许外部访问 |

### 宝塔面板

安全 → 防火墙 → 放行端口 8000。

## 八、文件说明

```
psychobot-server/
├── .env                          # API 配置（已填好）
├── Dockerfile.server             # Docker 镜像定义
├── docker-compose.server.yml     # Docker 编排文件
├── .dockerignore                 # Docker 构建排除
├── deploy.sh                     # 一键部署脚本
├── start.sh                      # 启动服务
├── stop.sh                       # 停止服务
├── psychobot.service             # systemd 开机自启配置
├── 服务器部署说明.md              # 本文件
├── pyproject.toml                # Python 依赖
├── uv.lock                       # 依赖锁定
├── alembic.ini                   # 数据库迁移配置
├── src/                          # 后端源码
│   └── psych_support_bot/
│       ├── app.py                # FastAPI 入口
│       ├── main.py               # 启动入口
│       ├── static/index.html     # 网页界面
│       └── ...
├── data/                         # 知识库数据
└── migrations/                   # 数据库迁移
```

## 九、常见问题

### 1）访问不了网页

按顺序排查：

```bash
# 检查容器是否在运行
docker ps

# 查看容器日志有没有报错
docker compose -f docker-compose.server.yml logs

# 检查服务器本地能不能访问
curl http://127.0.0.1:8000/health

# 检查防火墙
sudo ufw status
# 或
sudo firewall-cmd --list-ports
```

最常见的原因：
- 安全组没有放行 8000 端口
- 服务器防火墙拦截了
- 容器没有正常启动

### 2）容器启动失败

查看日志：

```bash
docker compose -f docker-compose.server.yml logs
```

常见原因：
- `.env` 文件不存在或格式错误
- 8000 端口被其他程序占用

### 3）端口被占用

查看谁在占用 8000 端口：

```bash
sudo lsof -i :8000
# 或
sudo ss -tlnp | grep 8000
```

要么关掉占用的程序，要么改 `docker-compose.server.yml` 里的端口。

### 4）API 调用失败

检查 `.env` 里的配置是否正确：

```bash
cat .env
```

确认 Key 没有过期、网络能访问到 API 服务。

## 十、不用 Docker 的部署方式（备选）

如果服务器装不了 Docker，也可以直接用 Python 跑：

```bash
# 安装 uv
curl -LsSf https://astral.sh/uv/install.sh | sh
source ~/.bashrc

# 安装依赖
cd /opt/psychobot
uv sync

# 前台运行（测试用）
uv run uvicorn psych_support_bot.app:app --host 0.0.0.0 --port 8000

# 后台运行
nohup uv run uvicorn psych_support_bot.app:app --host 0.0.0.0 --port 8000 > server.log 2>&1 &

# 配合 systemd 自启
# 需要修改 ExecStart 为:
# ExecStart=/root/.local/bin/uv run uvicorn psych_support_bot.app:app --host 0.0.0.0 --port 8000
```
