# 心理学Bot 使用说明（完全小白版）

这份说明默认你**完全不懂代码**，只要按顺序操作即可。

## 一、你会得到什么

这个项目已经带好下面几个关键文件：

- `一键启动.bat`：双击后会自动安装依赖、启动服务、打开网页
- `一键停止服务.bat`：双击后会关闭后台服务
- `src/psych_support_bot/static/index.html`：网页界面
- `.env` 或 `.env.example`：配置文件，用来填写 API Key

## 二、你的电脑需要先准备什么

只需要准备 **Python 3.12 或更高版本**。

### 1）检查有没有 Python

按下面步骤操作：

1. 按键盘 `Win + R`
2. 输入 `cmd`
3. 回车
4. 在黑色窗口里输入：

```bash
python --version
```

如果能看到类似 `Python 3.12.7`，就说明可以继续。

如果提示没有这个命令，就去下载安装：

- 官网：`https://www.python.org/downloads/`

安装时一定要注意：

- **勾选 `Add Python to PATH`**

## 三、第一次启动怎么做

### 最简单的方法

直接双击：

- `一键启动.bat`

它会自动做这些事：

1. 检查 Python 是否安装
2. 检查 `uv` 是否安装，没有就自动安装
3. 自动安装项目依赖
4. 自动启动本地服务
5. 自动打开浏览器网页

正常情况下，最后会自动打开这个地址：

- `http://127.0.0.1:8000`

如果浏览器没有自动打开，你就手动把上面这个地址复制到浏览器地址栏里打开。

## 四、API Key 要不要填

### 1）如果你只是想先看网页能不能跑起来

可以先不填，先启动试试看。

### 2）如果你希望 AI 正常调用大模型

建议填写 API Key。

操作方法：

1. 找到项目根目录下的 `.env` 文件
2. 右键它，选择“打开方式” → “记事本”
3. 找到这一行：

```text
OPENAI_API_KEY=
```

4. 在等号后面填入你的 Key，例如：

```text
OPENAI_API_KEY=sk-xxxxxxxxxxxxxxxx
```

5. 保存文件
6. 重新双击 `一键启动.bat`

### 3）其他配置要不要改

普通用户先不要改。默认配置通常已经够用了。

## 五、平时怎么启动

以后每次使用时，只需要：

1. 双击 `一键启动.bat`
2. 等待浏览器自动打开
3. 开始在网页里使用

## 六、怎么关闭

有两种方法：

### 方法 A：最简单

直接关闭名字叫：

- `Psych Support Bot Server`

的黑色命令行窗口。

### 方法 B：更省事

双击：

- `一键停止服务.bat`

## 七、常见问题

### 1）双击启动后没反应

先看有没有弹出黑色窗口。

- 如果一闪而过，通常是 Python 没装好
- 请重新安装 Python，并确认勾选 `Add Python to PATH`

### 2）提示依赖安装失败

通常是网络问题。

你可以：

1. 检查网络
2. 再次双击 `一键启动.bat`

### 3）网页打不开

手动打开浏览器，访问：

- `http://127.0.0.1:8000`

如果还是打不开：

1. 看 `Psych Support Bot Server` 窗口有没有报错
2. 确认没有被杀毒软件或防火墙拦截
3. 确认 8000 端口没有被别的软件占用

### 4）AI 回复异常，或者没有大模型效果

重点检查：

- `.env` 里有没有正确填写 `OPENAI_API_KEY`
- `OPENAI_BASE_URL` 和 `OPENAI_MODEL` 是否与你的服务匹配

### 5）服务已经开着，又重复启动了怎么办

不用担心。`一键启动.bat` 会优先检查服务是否已经运行。

如果已经在运行，它会直接打开网页，不会重复启动很多份。

## 八、给完全不懂技术的建议

你只记住下面 3 句话就够了：

1. **先装 Python**
2. **双击 `一键启动.bat`**
3. **用完双击 `一键停止服务.bat`，或者关掉黑色窗口**

## 九、项目主要文件说明

如果你以后要找文件，大概记这几个：

- `一键启动.bat`：启动
- `一键停止服务.bat`：停止
- `.env`：填 API Key
- `使用说明-小白版.md`：本说明
- `src/psych_support_bot/static/index.html`：网页页面
- `src/psych_support_bot/app.py`：后端入口配置

## 十、如果你要打包发给别人

建议把整个项目文件夹一起发给对方，不要只发某一个文件。

对方拿到后，也只需要：

1. 解压
2. 安装 Python
3. 双击 `一键启动.bat`

就可以使用。
